appId: com.operators.relay
productName: 'Relay'
asar: true
electronFuses:
  runAsNode: false
  enableNodeOptionsEnvironmentVariable: false
  enableNodeCliInspectArguments: false
  # enableEmbeddedAsarIntegrityValidation intentionally OFF: electron-builder signs
  # the unpacked native modules (better-sqlite3) after computing asar integrity
  # hashes, so the fuse trips at startup ("ASAR Integrity Violation") on every
  # signed build. Re-enable once builder computes integrity post-signing.
  enableEmbeddedAsarIntegrityValidation: false
  onlyLoadAppFromAsar: true
  resetAdHocDarwinSignature: true
electronLanguages:
  - en-US
files:
  - dist/**
  - package.json
  - resources/**
  - '!node_modules/fsevents/**'
  - '!**/*.{md,ts,map,log}'
  - '!**/LICENSE'
  - '!**/.{git,github,vscode,idea}'
  - '!**/tsconfig*.json'
  - '!**/.eslintrc*'
  - '!**/.prettierrc*'
  - '!**/*.{test,spec}.{js,jsx,ts,tsx}'
  - '!**/tests/**'
  - '!**/test-results/**'
  - '!**/playwright*'
  - '!**/vitest*'
directories:
  output: release
  buildResources: resources
npmRebuild: true
removePackageScripts: true
win:
  target:
    - target: portable
      arch:
        - x64
  artifactName: 'Relay.exe'
  icon: build/icon.ico
  requestedExecutionLevel: 'asInvoker'
  extraResources:
    - from: resources
      to: data
      filter:
        - '**/*'
        - '!pocketbase/**'
    - from: 'resources/pocketbase/win32-x64/'
      to: 'pocketbase/win32-x64/'
compression: 'maximum'
electronDownload:
  cache: '.electron'

mac:
  target:
    - target: dmg
      arch:
        - arm64
        - x64
  artifactName: 'Relay-${arch}.dmg'
  category: public.app-category.developer-tools
  icon: build/icon.icns
  hardenedRuntime: true
  gatekeeperAssess: true
  notarize: true
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  extraResources:
    - from: resources
      to: data
      filter:
        - '**/*'
        - '!pocketbase/**'
    - from: 'resources/pocketbase/darwin-arm64/'
      to: 'pocketbase/darwin-arm64/'
    - from: 'resources/pocketbase/darwin-x64/'
      to: 'pocketbase/darwin-x64/'
